---
import Layout from '../layouts/Layout.astro';
import Slider from '../components/Slider.jsx';
import Navbar from '../components/Navbar.astro';

---

<Layout title="La Pasteleria de La Postreria.">
	<Navbar />
	<main>
		<header>
			<div class="header-outline"/>
				<div class="container header-comp">
					<a href="#about" class="see-more">Ver Más <img src="/arrow-down.svg" alt="Arrow Icon"></a>
					<img src="/header-image.png" alt="Panqué de nuez" class="header-image">
				</div>
		</header>
		<section id="about">
			<div class="container">
				<div class="about-comp">
					<h2>Porque Pastelería</h2>
					<p>Dentro de nuestras sucursales podrás encontrar fácil y rápido, toda la variedad de pasteles de los restaurantes de La Postreria 77, productos exclusivos de La Pastelería, y artículos para decorar tus pasteles.</p>
				</div>
			</div>
		</section>
		<!-- <Slider client:load /> -->
	</main>
</Layout>

<style>

	/* Header Section */
	header {
		position: relative;
		height: 100dvh;
		width: 100%;
		display: flex;
		align-items: end;
		overflow-x: clip;
	}
	.header-outline {
		position: absolute;
		inset: calc(var(--page-padding) / 2);
		border: 1px solid ;
		z-index: -1;
	}
	.header-comp {
		display: flex;
		align-items: end;
		width: 100%;
	}
	.see-more {
		min-width: 30%;
	}
	.header-image {
		object-fit: cover;
		margin-bottom: -10%;
	}
	.see-more {
		opacity: 0.5;
		display: flex;
		gap: 0.25rem;
		align-items: end;
		color: var(--color-white);
		text-decoration: none;
		text-transform: uppercase;
		transition: all 0.2s ease-out;
	}
	.see-more img {
		width: 1.25rem;
		transition: all 0.3s ease-out;
	}
	.see-more:hover img {
		transform: translatey(0.15rem);
	}
	.see-more:hover {
		opacity: 1;
	}

	/* About Section */
	.about-comp {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 5rem;
		margin: 5rem 0;
	}
	.about-comp h2 {
		flex-shrink: 0;
		min-width: 40%;
	}

</style>
